"""Version identifiers shared by packaging, runtime, configuration, and storage."""

# This is the single source of truth for the application release version.
__version__ = "3.2.0"

# Data-format versions evolve independently from application releases.
SCHEMA_VERSION = "3.3"
CONFIG_VERSION = "1"
